// Package flashblocks holds the XLayer Flashblocks E2E scenarios. Each test
// brings up the XLayer Flashblocks devnet (one producer sequencer with rpc1/rpc2
// relays) through the shared harness. The scenarios are Go ports of the
// in-scope cases from the legacy Rust suite in crates/tests/flashblocks-tests/main.rs.
package flashblocks
