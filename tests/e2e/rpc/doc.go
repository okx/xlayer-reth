// Package rpc holds the XLayer JSON-RPC E2E scenarios. Each test brings up the
// XLayer single-chain devnet through the shared harness and drives the sequencer
// and validator RPCs; the scenarios are Go ports of the RPC cases from the legacy
// Rust suite in crates/tests/e2e-tests/main.rs.
package rpc
